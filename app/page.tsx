'use client';
export default function Page(){ return (<main style={{padding:20}}><h1>POE Web</h1><form onSubmit={(e: React.FormEvent<HTMLFormElement>)=>{e.preventDefault(); fetch('/api/session/start',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({})}).then(r=>r.json()).then(d=>{ localStorage.setItem('poe.sessionId',d.sessionId); location.href='/' + d.sessionId;});}}><button type='submit'>Start</button></form></main>); }
